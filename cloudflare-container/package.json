{
  "name": "carbonscope-backend-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker entrypoint for the CarbonScopes backend container",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.14.0"
  }
}
